name: CD - Build and Deploy

on:
  push:
    branches: [ main ]
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build.outputs.digest }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v1
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'staging')
    environment:
      name: staging
      url: http://staging.cs4445.example.com

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to staging (simulation)
        run: |
          echo "Deploying to staging environment"
          echo "Image: ${{ needs.build-and-push.outputs.image-tag }}"
          echo "Digest: ${{ needs.build-and-push.outputs.image-digest }}"

          # In real deployment, you would:
          # 1. SSH into staging server
          # 2. Pull new Docker image
          # 3. Run docker compose up with new image
          # 4. Run health checks

          # Example deployment command (commented):
          # ssh user@staging-server << 'EOF'
          #   cd /app/cs4445-sub-server
          #   docker compose pull
          #   docker compose up -d
          #   docker compose ps
          # EOF

      - name: Run health check
        run: |
          echo "Running health checks on staging"
          # In real deployment:
          # curl -f http://staging.cs4445.example.com/actuator/health || exit 1

      - name: Run smoke tests
        run: |
          echo "Running smoke tests"
          # Example smoke test
          # curl -X POST http://staging.cs4445.example.com/api/v1/fakePacket \
          #   -H "Content-Type: application/json" \
          #   -d '{"packetId":"ci-test","cpuIntensity":2,"ramIntensity":2}'

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build-and-push, deploy-staging]
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'production')
    environment:
      name: production
      url: http://cs4445.example.com

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to production (simulation)
        run: |
          echo "Deploying to production environment"
          echo "Image: ${{ needs.build-and-push.outputs.image-tag }}"
          echo "Digest: ${{ needs.build-and-push.outputs.image-digest }}"

          # In real deployment:
          # ssh user@prod-server << 'EOF'
          #   cd /app/cs4445-sub-server
          #   docker compose pull
          #   docker compose up -d --no-deps app
          #   docker compose ps
          # EOF

      - name: Run health check
        run: |
          echo "Running health checks on production"
          # curl -f http://cs4445.example.com/actuator/health || exit 1

      - name: Run smoke tests
        run: |
          echo "Running production smoke tests"

      - name: Notify deployment
        run: |
          echo "Deployment to production completed successfully"
          echo "Version: ${{ github.ref_name }}"
          # Send notification to Slack, Discord, email, etc.

  rollback:
    name: Rollback Deployment
    runs-on: ubuntu-latest
    if: failure() && (needs.deploy-staging.result == 'failure' || needs.deploy-production.result == 'failure')
    needs: [deploy-staging, deploy-production]

    steps:
      - name: Rollback to previous version
        run: |
          echo "Rolling back deployment"
          # ssh user@server << 'EOF'
          #   cd /app/cs4445-sub-server
          #   docker compose down
          #   docker tag cs4445-sub-server:previous cs4445-sub-server:latest
          #   docker compose up -d
          # EOF

      - name: Notify rollback
        run: |
          echo "Deployment rolled back due to failure"
          # Send alert notification
